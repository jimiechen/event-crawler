# TASK_首页股票列表升级.md

## 任务1：数据库迁移与模型更新
**输入**: 设计文档
**输出**: SQL脚本, 更新后的 `stock.py`
**验收标准**: 数据库表包含新字段，ORM模型包含新属性。

## 任务2：后端逻辑更新
**输入**: 更新后的模型
**输出**: 更新后的 `schemas.py`, `dashboard_service.py`
**验收标准**: 
- API支持按时间段筛选。
- API支持按异动分数排序。
- API返回包含新字段。

## 任务3：前端界面更新
**输入**: 更新后的API
**输出**: 更新后的 `index.html`
**验收标准**:
- 列表显示所有新指标。
- 支持时间筛选。
- 默认按分数排序。
